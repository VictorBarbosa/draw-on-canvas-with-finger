import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'draw-on-canvas-with-finger',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
